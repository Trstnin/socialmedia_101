<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="py-2 px-5 rounded-md bg-red-500 mb-5 inline-block">Logout</a>
        </div>
        
      <h3 class="text-3xl "><span class="text-light">Hello</span>, <%= user.fullname%> !!  &#x1F44B</h3>
      <h5 class="mb-5">You can create a post.</h5>

      <form action="/post" method="post">
        <input placeholder="What's on your mind? " type="textarea" class="block p-3 w-1/3  resize-none  bg-transparent border-2 border-zinc-800 rounded-md outline-none" name="content" required>
        <input type="submit" class=" w-40 px-5 py-3 block bg-blue-500 rounded-md mt-3 text-sm" value="Create a new post ">
      </form>
       <div class="Posts mt-20">
        <h3 class="text-zinc-400 mb-3">Your Posts</h3>

          <% user.posts.reverse().forEach((post) => {%>
            <div class="Posts_container ">
                <div class="post w-1/3 p-4 border-2 border-zinc-700 bg-zinc-800 rounded-md mt-2 mb-5">
                    <h3 class="text-blue-500 mb-2">@<%=user.username %></h3>
                    <p class="tracking-tight text-sm"><%=post.content%></p>
                    <div class="flex gap-3 mt-2">
                        <small class="text-red-400"><%=post.loves.length%> loves</small>
                        <small class="text-blue-500"><%=post.likes.length%> likes</small>
                    </div>
                  

                    <div class="btns mt-2 flex gap-5 mt-1">
                        <a href="/love/<%=post._id %>" class="text-red-400"><%= post.loves.indexOf(user._id) === -1 ? "Love" : "Unlove" %></a>
                        <a href="/like/<%=post._id %>" class="text-blue-500"><%=post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %></a>
                        <a href="/edit/<%=post._id%>" class="text-green-500">Edit</a>
                        <a href="/delete/<%=post._id%>" class="text-red-400">Delete</a>
                    </div>
                </div>
         <% })%>



        </div>
       </div>

       

    </div>
  </body>
</html>
